apiVersion: ://coreos.com
kind: ServiceMonitor
metadata:
  name: backend-daemon-monitor
  namespace: trading-desk
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: backend-daemon
  endpoints:
  - port: http
    path: /metrics
    interval: 10s
